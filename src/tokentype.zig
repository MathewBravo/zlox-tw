pub const TokenType = enum {
    left_paren,
    right_paren,
    left_brace,
    right_brace,
    comma,
    dot,
    minus,
    plus,
    semicolon,
    slash,
    star,
    bang,
    bang_equal,
    equal,
    equal_equal,
    less,
    less_equal,
    greater,
    greater_equal,
    identifier,
    string,
    number,
    // _token to get around reserved keywords
    and_token,
    class_token,
    else_token,
    false_token,
    fun_token,
    for_token,
    if_token,
    nil_token,
    or_token,
    print_token,
    return_token,
    super_token,
    this_token,
    true_token,
    var_token,
    while_token,
    eof,
};
